(()=>{
    let calenderspot;

    chrome.runtime.onMessage.addListener((obj, sender, response) => {
        const {type, isCalender} = obj;

        if(type === "alx"){
            
        }
    })
})();